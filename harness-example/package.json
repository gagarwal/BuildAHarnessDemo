{
  "name": "harness-example",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "A minimal agent harness that drives GitHub Copilot (via @github/copilot-sdk) to build a URL shortener — the code companion to what-is-a-harness.html.",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "start": "node harness.ts"
  },
  "dependencies": {
    "@github/copilot-sdk": "^1.0.5",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@types/node": "^26.1.0",
    "typescript": "^6.0.3"
  }
}
